<div class="container">
<div class="div">
  <h3>My Lists</h3>
</div>
    <% @lists.each do |list| %>
      <p><%= link_to "#{list.name}", list_path(list)%><p>
    <% end %>
      <%= link_to "Add List", new_list_path, class: "btn btn-dark"%>


</div>
